services:
  - type: web
    name: rennsz-landing-page
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production